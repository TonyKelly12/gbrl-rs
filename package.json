{
  "name": "meshforge",
  "version": "0.1.0",
  "private": true,
  "description": "MeshForge CNC control — Tauri desktop app",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "dev:mock": "cross-env MESHFORGE_MOCK=1 tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "cross-env": "^7"
  }
}
